/**
 * Shared configuration constants for the Mobile Hub.
 * These should match the Shadow CLI's reporting window parameters.
 */

export const REPORTING_WINDOW_MS = 60000;
export const DEFAULT_POLLING_INTERVAL_MS = 10000;
