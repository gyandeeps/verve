---
description: how to commit changes
---

// turbo-all

1. Run `npm run format` to ensure all files obey styling rules.
2. If formatting resulted in changes, stage them again with `git add .`
3. Execute the `git commit` command with the appropriate message.
