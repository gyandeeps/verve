---
description: how to commit changes
---

// turbo-all

1. Run `make format` (Go CLI and Mobile Hub/Docs) to ensure all files obey styling rules.
2. If any docs need updates do that else skip this step.
3. If formatting resulted in changes, stage them again with `git add .`
4. Execute the `git commit` command with the appropriate message.
